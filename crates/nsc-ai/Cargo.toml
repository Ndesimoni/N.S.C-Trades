# The AI validation layer. Phase 5, and gated off by default.
[package]
name    = "nsc-ai"
version.workspace = true
edition.workspace = true

[dependencies]
nsc-core   = { workspace = true }
tokio      = { workspace = true }
reqwest    = { workspace = true }
serde      = { workspace = true }
serde_json = { workspace = true }
chrono     = { workspace = true }
anyhow     = { workspace = true }
thiserror  = { workspace = true }
tracing    = { workspace = true }

# Phase 4: ONNX inference for the trained scorer. Uncomment when a model
# exists — there is nothing to load until research/ has produced one.
# ort = "2"

# Phase 4 only. Nothing here is part of the running system.
pandas>=2.2
pyarrow>=16.0          # parquet export from postgres
sqlalchemy>=2.0
psycopg[binary]>=3.2
lightgbm>=4.3          # the scorer — boosted trees, not deep learning
scikit-learn>=1.5      # splits, metrics, calibration
shap>=0.45             # which confluences actually matter
onnxmltools>=1.12      # export to ONNX for inference in nsc-ai
matplotlib>=3.9
jupyterlab>=4.2

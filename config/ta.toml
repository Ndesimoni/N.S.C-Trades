# ── How sensitive the chart reading is ─────────────────────────────────────
# These numbers decide what the bot *sees*. Test them in the backtester. Do
# not adjust them by eye because one chart looks nicer.

[swings]
# THE most important settings in the project. Levels, trendlines, Fibonacci
# and trend direction are ALL built from swing points.
#
# NO CANDLE COUNTING. A peak is not a peak because of how many candles sit
# either side of it — that passes a lazy rounded top with twenty quiet candles
# round it, and fails a sharp turn with four. What proves a peak is what price
# did afterwards.
#
# Every number here is a SHARE OF A MOVE, never a distance. That is why the
# same four work on the 4-hour and the daily, and on gold and EURUSD.

# How much of the run must be given back for a peak to prove itself on its own.
# Half of THAT move: a 300-point rally needs about 150 back, a 60-point rally
# about 30.
confirm_retracement = 0.5
# The shallower give-back that counts once price has TAKEN THE PEAK OUT.
#
# A strong move barely pauses. Insisting on half would read structure fine in
# chop and go blind in a clean trend, which is the market worth trading. Same
# number as the strong-trend Fibonacci level below, because it is the same
# situation.
shallow_retracement = 0.382
# How big a run must be next to recent ones, or it is not a move at all.
# Without it, half of a tiny run is a tinier pullback and a quiet afternoon
# fills with swings. Range worth trying: 0.5 to 0.75. Higher means fewer,
# bigger swings.
min_run_fraction    = 0.5
# How far back "recent" reaches, counted in runs. Against only the last run the
# test ratchets down — 200, 120, 72, 43 — each passing while the chain shrinks
# to nothing. Remembering five stops that.
run_memory_legs     = 5

[levels]
# How thick a level band is, as a fraction of a normal candle.
#
# The thickness is the SAME for every level on a timeframe. The band gets slid
# up and down to catch the most touches; it never gets stretched. That is how
# the levels on your charts are drawn — see docs/worksheets/levels.md.
#
# Too thin and a wick that overshoots by a hair misses the level. Too thick
# and every price is at every level.
band_atr_multiple = 0.5
# How thick a HAND-DRAWN level's band is, as a fraction of a normal candle on
# its own timeframe.
#
# The levels files hold one price per level. This turns that price into a band.
#
# TWO NUMBERS, NOT ONE. Measured off the trader's own gold chart: his weekly
# band is 0.36 of a normal weekly candle, his daily 0.63 of a normal daily one.
# Rounded here to what a pixel measurement can honestly support. A single
# multiple cannot produce both — on the higher timeframe he draws TIGHTER
# relative to how far price moves, not looser.
#
# Same numbers on every instrument. He draws with the same pen everywhere.
drawn_weekly_atr  = 0.35
drawn_daily_atr   = 0.60
drawn_h4_atr      = 0.60
# How many touches before a band counts as a real level at all. Below this,
# nothing is drawn and nothing is reported.
#
# This is a SEEING setting, which is why it lives here: one swing point on its
# own is not a level, it is a swing point.
min_touches       = 2
# Levels older than this many candles stop being considered. Roughly three
# years of weekly candles, two years of daily, three months of 4-hour — which
# is about as far left as you look on each.
max_age_bars      = 500
# How far clear of a BIGGER level a smaller one must sit to keep its own line,
# measured in the bigger band's own thickness.
#
#   0.0   only when they actually touch
#   0.5   also when you could not fit another band between them
#   1.0   anything within a full band's width
#
# Overlapping is not the only thing that makes two lines read as one. A daily
# band sitting just under a weekly band, clear of it, still looks like one
# thick line and tells you nothing the weekly did not.
#
# Measured in bands rather than points so it means the same on gold and on
# EURUSD. A weekly band is thick, so "close to a weekly" is a bigger distance
# than "close to a 4-hour" — which is right.
#
# A DRAWING rule. Nothing is thrown away: the level is still found, still
# counted, and the engine still knows two timeframes turned at that price.
absorb_gap_bands  = 1.5
# How far apart two levels on the SAME timeframe must sit, in that timeframe's
# own band-widths.
#
# THE CONSOLIDATION RULE. Price chops around one area for two years and turns a
# dozen times, so a level is found at every turn. You look at all of it and draw
# ONE line saying "price did something here".
#
# When two are too close, the one with MORE TOUCHES keeps the line — the price
# it actually turned at, not the middle of the area.
#
# The one crowded out is not deleted. And because only a DRAWN level can cover a
# smaller one, a weekly that lost its line stops hiding the daily at that price,
# so the daily draws itself instead. That is the demotion, and it needs no rule
# of its own.
#
# Measured off the trader's own gold weekly chart on 13 Aug 2026: his levels sit
# 400 to 800 apart, and a weekly band is about 125. Three bands is the tightest
# he draws.
min_separation_bands = 3.0

# NO strong_touches HERE. It moved to strategy.toml [levels].
#
# How many touches makes a level worth trading is a trading opinion, not part
# of finding one. nsc-ta reports the touch count and stops; whether four
# touches means "well defended" or "about to break" is decided by the rules,
# where it can be switched off and measured.

[proximity]
# What counts as "price is AT the level". In normal-candle multiples, never in
# pips, because 20 pips means something completely different on GBPJPY than on
# EURUSD.
at_level_atr_multiple = 0.25

[trendlines]
min_touches          = 3      # two points make a line; three makes it real
max_violation_closes = 0      # closes past the line that break it
min_bars_between     = 5      # stops neighbouring candles making fake lines

[fibonacci]
# Drawn automatically from the most recent confirmed move.
#
# FOUR LEVELS ONLY: 0.382, 0.5, 0.618 and 0.786. Nothing else gets drawn.
#
# THE GOLDEN ZONE, and the most important two. Where to look to get in: price
# sitting between them is the thing to pay attention to.
golden_zone = [0.5, 0.618]
# NOT AN ENTRY LEVEL — a reading. A pullback this shallow means the market
# barely paused, which is what a strong trend looks like. When price turns here
# rather than reaching the zone, the information is "this move is powerful".
#
# Same number as shallow_retracement in [swings] above, because it is the same
# belief written once.
strong_trend_level = 0.382
# Where stops get looked at — NOT ALWAYS, and never on its own. Other factors
# come into it, so nsc-ta draws this level and the invalidation layer in
# nsc-strategy decides whether the stop actually goes there. A stop placed by
# one line every time is a stop everybody can see.
stop_level  = 0.786
# Used for targets. NOT confirmed yet — these are the standard numbers, not
# this trader's.
extensions  = [1.272, 1.618]

[candles]
# EIGHT SHAPES: pin bar, engulfing, doji, belt-hold, tweezers, inside bar and
# star — with hammer and inverted hammer being the same shape as a pin bar
# pointing one way or the other.
#
# THIS LIST IS NOT FINISHED, AND IT IS NOT MEANT TO BE. There are more shapes
# being traded than have been named, and naming them is a job for the labelled
# trades in Phase 4, not for guesswork now. Adding a detector nobody asked for
# would put a rule in the bot that nobody could explain.
#
# THESE NUMBERS ARE TEXTBOOK, NOT YOURS. They were taken as standard defaults
# on 12 Aug 2026 so the detectors could be built, and they stay borrowed until
# a pair of charts — one you took, one you passed that looked the same —
# replaces them. See docs/worksheets/candles.md.
#
# Shape is measured as shares of a candle's OWN height, so a body that is a
# fifth of its candle is a fifth on EURUSD and a fifth on gold. Only two
# settings are in ATR, and both are about size rather than shape.

# ── Pin bar ──
# How many times the body the long wick must be.
pin_min_tail_to_body = 2.0
# The most of the candle the body may take.
pin_max_body_share   = 0.33
# The most the wick on the OTHER side may take. This is what keeps the body at
# the far end from the tail — without it, long wicks both sides would pass, and
# that shape means close to the opposite thing.
pin_max_nose_share   = 0.25

# ── Engulfing ──
# The least the first candle's body may be. Without it almost anything engulfs
# a doji and the word stops meaning anything. Bodies only — wicks are ignored,
# which is the standard definition.
engulfing_min_first_body_share = 0.1

# ── Doji ──
# The most of its candle a body may take and still be a doji.
doji_max_body_share         = 0.05
# A wick smaller than this counts as none at all. It is what separates a
# dragonfly from a long-legged doji.
doji_max_missing_wick_share = 0.05

# ── Belt-hold ──
# The most wick allowed on the OPENING side. In real forex there is nearly
# always a tick or two, so "no wick" cannot mean exactly zero.
belt_hold_max_open_wick_share = 0.05
# The least of the candle the body must take.
belt_hold_min_body_share      = 0.6
# How tall it must be, in normal candles. A tiny candle with the same
# proportions is a quiet minute, not a belt-hold.
belt_hold_min_atr_multiple    = 1.0

# ── Tweezers ──
# How close two highs must be to count as the same price, in normal candles.
# Two candles never share a high to the tick.
tweezer_tolerance_atr = 0.05

# ── Inside bar ──
# No settings. Either the second candle's range is inside the first or it is
# not, and there is no threshold to get wrong.

# ── Star ──
# The most of its candle the MIDDLE body may take. This is what makes it a
# stall rather than a third push.
star_max_middle_body_share = 0.2
# The least the two OUTER bodies must take. Both have to be real moves, or the
# shape is three shrugs in a row.
star_min_outer_body_share  = 0.5
# How far into the first candle's body the third must close, as a share of that
# body. Half means it gave back half the push.
#
# This is the test that does the work. Without it, any small candle followed by
# any down candle would be an evening star — a pause and a reversal would look
# the same.
star_min_close_into_first  = 0.5

[indicators]
atr_period = 14      # this is the yardstick for every setting above
rsi_period = 14

[structure]
# TAKING AN OLD HIGH OUT IS NOT ENOUGH. Price has to cross it AND carry this
# share of the run that made it past — measured from the high itself, not from
# where the pullback started.
#
# A run from 1900 to 2100 is 200 points. At 0.4, price has to reach 2180 before
# that high counts as taken. Poke through by 5 points and stall and the high
# was touched, not taken.
#
# That poke is the most common trap on a chart: it looks like a breakout, it
# pulls buyers in, and price turns straight back down. Without this the bot
# reads it as a higher high and goes looking for a long at the worst moment.
#
# Range worth trying: 0.4 to 0.5. It is a FLOOR, not a target — carrying much
# further is the same answer arrived at more convincingly, and how far it went
# is kept on the break for the rules to weigh.
#
# Lower lows use the same number, mirrored. One rule with a direction passed
# in, because an uptrend and a downtrend judged by different rules is how a bot
# ends up bullish and bearish about the same chart on the same day.
#
# This replaces bos_atr_multiple, which measured the same thing against normal
# candle size. A share of the run is the better yardstick: what counts as real
# follow-through should change with the size of the move in front of you, not
# with how big candles happen to be this week.
min_follow_through = 0.4

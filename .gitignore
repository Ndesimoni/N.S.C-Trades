# Rust build output
/target

# Local secrets — NEVER commit. Copy .env.example instead.
.env
*.pem

# Backtest artefacts and generated charts
/out/
/data/cache/
*.png
!docs/**/*.png

# Python research sidecar (Phase 4)
research/.venv/
research/**/__pycache__/
research/**/*.parquet
research/**/*.onnx
.ipynb_checkpoints/

# OS noise
.DS_Store

# The last cards the bot drew. Handy to look at, not worth keeping.
preview/

# Pairs he has stopped watching. His levels, kept aside so a tap on a phone is
# not final — but not part of the project.
config/pairs/removed/

# The half-written file an atomic save leaves if the machine dies mid-write.
*.toml.part

# Years of candles, pulled once so a detector can be worked on offline.
# Big, re-fetchable, and not the record — that is Postgres, when it lands.
/data/history/

# migrations/

Applied in filename order.

The tables are built around one idea: **every signal the bot ever sends must
stay connected to what the market did and what you thought of it.**

Those three things together — what the bot saw, what happened, what you said —
are the training data. Phase 4 is impossible without them, which is why the
tables exist from day one instead of being added later.
